import FormPath from '../../components/forms/FormPath';

function New() {
  return (
    <div>
      <h1 style={{ color: 'white' }}>Create New Learning Path</h1>
      <FormPath />
    </div>
  );
}

export default New;
// The NavBar component is a custom component that we created to display the navigation bar at the top of the page. The NavBar component is used in the index.js file to display the navigation bar at the top of the home page. The NavBar component is also used in the new.js file to display the navigation bar at the top of the new page. The NavBar component is a reusable component that can be used in multiple pages to display the navigation bar at the top of the page.
